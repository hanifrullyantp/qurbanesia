VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: if `true`, login uses the direct GoTrue /token endpoint (see `src/lib/goTruePasswordLogin.ts`).
# Default is standard `supabase.auth.signInWithPassword`.
# VITE_USE_DIRECT_GOTRUE_LOGIN=false

# Needed only for local admin scripts (NEVER commit real value)
SUPABASE_SERVICE_ROLE_KEY=

